import { type SingleItem, type Options } from './types';
export declare const DEFAULT_OPTIONS: Options;
export declare const CSS_CLASSES: {
    namespace: string;
    container: string;
    separator: string;
    submenu: string;
    hidden: string;
    icon: string;
    zoomIn: string;
    zoomOut: string;
    unselectable: string;
};
export declare const DEFAULT_ITEMS: SingleItem[];
